# KinEcho 依赖审计豁免清单

更新时间：2026-05-08

## 当前门禁

- 根项目：`npm audit --audit-level=high` 必须通过。
- 小程序：`npm --prefix miniapp audit --audit-level=high` 必须通过。
- CI：`.github/workflows/ci.yml` 已纳入 `npm run audit:security`。

## 已治理的 high/critical 来源

| 来源 | 处理方式 | 验证 |
| --- | --- | --- |
| `vm2` | `miniapp/package.json` override 到 `3.11.2` | `npm --prefix miniapp audit --audit-level=high` 通过 |
| `axios` | override 到 `1.16.0` | `npm --prefix miniapp audit --audit-level=high` 通过 |
| `html-minifier` | 用本地兼容包装包转到 `html-minifier-terser` | `npm --prefix miniapp run build:weapp` 通过 |
| `download-git-repo` / `git-clone` | 用本地禁用包隔离 Taro 模板下载能力，CI 仅允许构建路径 | `npm --prefix miniapp ci` 与构建通过 |
| `postcss-url` / `minimatch` | 当前 CSS 未使用 `url()` 资产，使用本地 no-op 包隔离该构建插件 | `rg -n "url\\(" miniapp/src miniapp/config miniapp/postcss.config.js` 无结果；构建通过 |
| `serialize-javascript`、`got`、`glob`、`http-cache-semantics` | override 到审计可接受版本 | `npm --prefix miniapp audit --audit-level=high` 通过 |

## 临时豁免项

| 漏洞来源 | 严重级别 | 影响面 | 临时缓解 | 到期时间 |
| --- | --- | --- | --- | --- |
| `@tarojs/webpack5-runner -> miniprogram-simulate -> postcss@7.0.39` | moderate | 仅存在于小程序构建工具链，不进入小程序运行包；当前项目不在 CI 中运行 miniprogram simulator，也不处理外部不可信 CSS 输入 | 发布门禁按 high/critical 阻断；保留完整 `npm audit` 输出；后续跟踪 Taro 4.x 或 `miniprogram-simulate` 修复版本，若无修复则评估移除模拟器依赖或升级构建链 | 2026-06-30 |

## 复核要求

1. 每次升级 Taro 或小程序构建链后重新运行 `npm --prefix miniapp audit --json`。
2. 若出现任何 critical/high，必须先修复或隔离，不能沿用本豁免。
3. 到期前需重新评估 `miniprogram-simulate` 的修复状态，并更新本清单。
