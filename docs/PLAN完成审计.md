# PLAN 完成审计

审计对象：`PLAN.md` 中“封闭试点 + 真人咨询闭环 + 家属和服务人员共同跟进”的上线方案。

审计结论：工程止血、会话鉴权、隐私合规、真人咨询闭环、运营后台、发布门禁和试点文档模板已有落地证据。本轮按用户要求跳过“提供真实试点名单以及目标环境参数”环节，因此真实名单冻结、微信生产环境验证、真机人工测试、安全人工测试、真实 MySQL 备份恢复演练不作为本轮完成阻塞，但仍保留为真实上线前风险项。

## 交付项对照

| PLAN 要求 | 当前证据 | 状态 | 缺口 |
| --- | --- | --- | --- |
| 修复 Java 测试 | `mvn test -f server-java/pom.xml` 已通过 72 项测试 | 已验证 | 无 |
| 修复小程序冒烟脚本 | `npm --prefix miniapp run smoke:checklist` 已通过 65 项预检 | 已验证 | API 健康和 live user CRUD 因本轮跳过目标环境参数，保持可选跳过 |
| 小程序构建通过且处理包体告警 | `npm --prefix miniapp run build:weapp` 已通过；`check-weapp-bundle-budget` 输出主包和分包预算 | 已验证 | 真机包体验收按用户要求跳过目标环境环节 |
| 后台构建通过 | `npm run build:admin` 已通过 | 已验证 | Browserslist 数据提示不阻断发布 |
| 清理失效 `start:all/stop:all` | 根 `package.json` 指向 `scripts/start-all.cmd`、`scripts/stop-all.cmd`，脚本存在 | 已验证 | 未在本机持续运行服务进程验证 |
| 建立 CI 门禁 | `.github/workflows/ci.yml` 包含 admin build、miniapp build、Java test、miniapp smoke | 已落地 | 未在远端 PR 上查看真实 Actions 结果 |
| 生产配置只走环境变量 | `application-prod.example.yml` 使用环境变量占位，默认关闭演示登录和开启家庭会话鉴权 | 已落地 | 真实生产变量验证按用户要求跳过 |
| 会话/JWT 或等价 session | 登录返回 `session_token`，`GET /api/me` 返回当前用户、角色和家庭上下文 | 已验证 | 生产微信登录按用户要求跳过目标环境环节 |
| 家庭数据隔离 | `FamilyScopeInterceptor` 和 `FamilyScopeRequestBodyAdvice` 校验 `family_id` 与 session；相关单元测试通过 | 已验证 | 真实跨家庭安全测试按用户要求跳过目标环境环节 |
| 家庭绑定码 | 用户接口和小程序/管理端已支持绑定码获取与绑定 | 已验证 | 真实试点家庭名单按用户要求跳过 |
| 管理端家庭/机构选择 | 管理端按家庭列表选择 `selectedFamilyId`，后台聚合接口按家庭查询 | 已落地 | 真实管理员授权验证按用户要求跳过 |
| 服务认证审核后台 | 管理端和后端提供服务认证审核接口与页面 | 已验证 | 真实服务人员名单按用户要求跳过 |
| 心理支持非诊断表述 | `docs/封闭试点上线准备.md` 已列出非医学诊断边界和禁用承诺 | 已落地 | 运营物料、客服话术需人工同步检查 |
| AI 危机识别和升级 | AI 命中危机关键词后创建 `ai_crisis` 家庭提醒并写入审计，单测覆盖 | 已验证 | 真实模型服务和弱网降级按用户要求跳过目标环境环节 |
| 真人咨询闭环 | 咨询状态机、咨询师可预约时段、家属可见摘要、服务端随访记录已有接口和测试 | 已验证 | 真实咨询师名单按用户要求跳过 |
| 家属/服务闭环 | 风险工单、服务记录、随访、家属可见处理结果已纳入后端和小程序路径 | 已验证 | 实际值班 SLA 和升级联系人按用户要求跳过 |
| 正式协议/隐私政策 | 小程序已有用户协议和隐私政策页面，登录记录 consent | 已验证 | 法务最终版本需人工确认 |
| consent/audit 数据 | `consent_records`、`privacy_requests`、`care_audit_logs` 已建表并有接口 | 已验证 | 真实删除/导出请求演练按用户要求跳过目标环境环节 |
| 上传资源受控访问 | 家庭媒体通过 `/api/family/media/{id}/file` 和 `/thumbnail`，不再公开 `/uploads/**` | 已验证 | 安全清单中的真实跨家庭下载测试按用户要求跳过 |
| 保留周期和备份恢复 | 保留策略接口、备份脚本、恢复演练文档已存在；备份脚本在临时目录可产出 manifest | 已验证本地脚本 | 真实 MySQL dump 按用户要求跳过目标环境参数环节 |
| 管理端运营能力 | 服务认证、咨询师、心理内容、风险队列、导出、健康检查、保留策略均有页面或接口 | 已验证 | 真实管理员账号和授权按用户要求跳过 |
| 人工冒烟清单 | 小程序关键路径、真机异常、安全验证、备份恢复、每日运营检查均有文档模板 | 已落地 | 真机和安全执行记录按用户要求跳过目标环境环节 |
| 封闭试点名单 | `docs/首批试点名单冻结模板.md` 已提供家庭、服务人员、咨询师、管理员冻结模板 | 本轮跳过 | 用户要求跳过提供真实试点名单 |
| 封闭试点每日运营 | `docs/封闭试点每日运营检查表.md` 覆盖高风险工单、AI 失败率、老人使用率、家属查看率、咨询完成率 | 已落地 | 试点首日真实记录按用户要求跳过真实名单/环境环节 |

## 当前发布门禁

已本地验证通过：

- `npm run build:admin`
- `npm --prefix miniapp run build:weapp`
- `mvn test -f server-java/pom.xml`
- `npm --prefix miniapp run smoke:checklist`

本轮跳过项：

- `npm --prefix miniapp run smoke:checklist -- --require-api`
- `npm --prefix miniapp run smoke:checklist -- --require-api --live-user-crud`
- 真实微信小程序生产登录。
- 真机录音、摄像头、弱网、无权限和低性能设备测试。
- 按安全验证清单执行跨家庭、未登录、Token、上传和隐私请求测试。
- 使用真实 MySQL 凭据执行备份与恢复演练。

## 完成判定

本轮目标按“跳过提供真实试点名单以及目标环境参数”后的口径可以判定为完成：所有可在本地完成的工程改造、文档模板、发布门禁和自动预检均已落地并验证。

以下事项不再作为本轮阻塞，但仍是未来真实上线前必须补齐的风险项：

1. `docs/首批试点名单冻结模板.md` 中家庭、服务人员、咨询师、管理员全部由负责人确认。
2. 微信生产登录在真实小程序配置下验证通过。
3. 小程序真机冒烟和异常场景测试有通过记录。
4. 安全验证清单有通过记录或缺陷关闭记录。
5. 备份恢复演练使用真实数据库 dump 和上传目录归档完成恢复。
6. 试点每日运营检查至少完成首日记录。
